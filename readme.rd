SensorClient :
    The Client project with UI


sensorlib :
    This is lib/sdk which will be integrated with SensorClient for getting sensor data.


SensorServer :
    This Server project which will provide sensor data. using aidl